# Server URL (defaults to http://localhost:3000)
# MCP_URL=https://your-server.com

# Note: Eventbrite deprecated their public event search API in 2020.
# The app currently uses Luma (no auth required) as the live event source.
